import { Palette, ThemeTypography } from '../structure';
import { elevation } from './elevation';
export declare function createTheme(palette: Palette, typography: ThemeTypography): {
    components: {};
    elevation: typeof elevation;
    palette: Palette;
    shape: {
        borderRadius: number;
    };
    spacing: {
        unit: number;
    };
    typography: ThemeTypography;
};
