/// <reference types="react" />
import { ComponentStateProps } from '../../index';
export interface EmptyStateProps extends ComponentStateProps {
    /**
     * Used to locate this view in end-to-end tests.
     */
    testID?: string;
}
/**
 * # 📭 EmptyState
 *
 * A generic state used on screens or widgets that are empty or have no data.
 *
 * ## Usage
 * ```jsx
 * <EmptyState/>
 * ```
 */
export declare const EmptyState: {
    (props: EmptyStateProps): JSX.Element;
    displayName: string;
};
