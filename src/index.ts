import { createPlugin } from '@bluebase/core';

export default createPlugin({
	description: 'A BlueBase plugin boilerplate!',
	key: 'plugin-victorychart',
	name: 'Untitled Plugin',
	version: '1.0.0',
});
